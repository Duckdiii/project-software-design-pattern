$kind: http-request
name: '07 - Tax Reduced 5 Percent'
method: POST
url: 'http://localhost:5000/api/checkout'
order: 7000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "items": [
        { "name": "Laptop", "price": 2000000 }
      ],
      "taxType": "reduced"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 200", function () {
          pm.response.to.have.status(200);
      });
      pm.test("Tax is 5% of subtotal", function () {
          const json = pm.response.json();
          pm.expect(json.tax).to.eql(100000);
      });
    language: text/javascript
